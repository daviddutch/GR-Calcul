@model Nullable<DateTime>
@if ( Model.HasValue )
{
    @Html.TextBox( "" , String.Format( "{0:MM/dd/yyyy}" , Model.Value ) , new { @class = "date" } )
    
}
else
{
    @Html.TextBox( "" , String.Format( "{0:MM/dd/yyyy}" , DateTime.Now ) , new { @class = "date" } ) 
}